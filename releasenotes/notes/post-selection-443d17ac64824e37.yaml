---
features:
  - |
    Add the transpiler passes required to pre-process the circuits, as well as the
    ``PostSelector`` and ``PostSelectionSummary`` objects required to post-process
    the results.
upgrade:
  - |
    This addition required bumping the minimum version of Qiskit SDK to 2.0.